import { createSlice, nanoid } from '@reduxjs/toolkit'

const initialState = {
  test: 'sdsds'
}

export const { reducer, actions } = createSlice({
  name: 'calendar',
  initialState,
  reducers: {

  }
})
